{
  "name": "@eth-optimism/ERC20-Example",
  "version": "0.0.1-alpha.1",
  "description": "Basic example of how to test a basic token contract with Waffle in the OVM",
  "scripts": {
    "clean": "rimraf build",
    "build": "mkdir -p build && waffle waffle-config.json",
    "test": "yarn build && mocha 'test/*.spec.js' --timeout 10000",
    "all": "yarn clean && yarn test"
  },
  "keywords": [
    "optimism",
    "rollup",
    "optimistic",
    "ethereum",
    "virtual",
    "machine",
    "OVM",
    "ERC20",
    "waffle"
  ],
  "homepage": "https://github.com/ethereum-optimism/ERC20-Example#readme",
  "license": "MIT",
  "author": "Optimism PBC",
  "repository": {
    "type": "git",
    "url": "https://github.com/ethereum-optimism/ERC20-Example.git"
  },
  "devDependencies": {
    "ethereum-waffle": "^3.0.0",
    "mocha": "^7.0.1",
    "rimraf": "^2.6.3",
    "solc": "0.5.16",
    "chai": "^4.3.0"
  },
  "publishConfig": {
    "access": "public"
  }
}
