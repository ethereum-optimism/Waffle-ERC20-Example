{
  "name": "@eth-optimism/ERC20-Example",
  "version": "0.0.1-alpha.1",
  "description": "Basic example of how to test a basic token contract with Waffle in the OVM",
  "scripts": {
    "clean": "rimraf build",
    "build": "yarn build:evm && yarn build:ovm",
    "build:evm": "waffle waffle.json",
    "test:evm": "yarn build:evm && TEST_MODE=EVM mocha 'test/*.spec.js' --timeout 10000",
    "all:evm": "yarn clean && yarn test:evm",
    "build:ovm": "waffle waffle-ovm.json",
    "test:ovm": "yarn build:ovm && TEST_MODE=OVM mocha 'test/*.spec.js' --timeout 50000",
    "all:ovm": "yarn clean && yarn test:ovm"
  },
  "keywords": [
    "optimism",
    "rollup",
    "optimistic",
    "ethereum",
    "virtual",
    "machine",
    "OVM",
    "ERC20",
    "waffle"
  ],
  "homepage": "https://github.com/ethereum-optimism/ERC20-Example#readme",
  "license": "MIT",
  "author": "Optimism PBC",
  "repository": {
    "type": "git",
    "url": "https://github.com/ethereum-optimism/ERC20-Example.git"
  },
  "devDependencies": {
    "@eth-optimism/plugins": "^0.0.12",
    "@eth-optimism/solc": "^0.5.16-alpha.7",
    "ethereum-waffle": "^3.0.0",
    "solc": "0.5.16",
    "mocha": "^7.0.1",
    "rimraf": "^2.6.3"
  },
  "publishConfig": {
    "access": "public"
  }
}
